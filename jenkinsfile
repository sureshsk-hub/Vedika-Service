node{
  stage ('Clone From Git') {

    git url: 'https://github.com/sureshsk-hub/Vedika-Service.git'
    } 
   stage('Gradle Build') {
     def gradleHome = tool name: 'gradle', type: 'gradle'
     def gradleCMD = "${gradleHome}/bin/gradle"
     sh "${gradleCMD} clean build"
    }

}
